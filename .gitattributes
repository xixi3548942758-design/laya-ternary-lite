# 统一用 LF 存仓库，避免 Windows 的 CRLF 影响 Linux/macOS 用户
* text=auto eol=lf

*.bin binary
*.safetensors binary
*.png binary
*.jpg binary

# 三值权重约 88 MiB，GitHub 单文件上限 100 MiB，可以直接提交。
# 若后续加入更大的权重，取消下面注释改用 Git LFS：
# *.bin filter=lfs diff=lfs merge=lfs -text
